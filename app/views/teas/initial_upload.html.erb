<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-body text-center">
          <h2 class="card-title mb-4">Welcome to Tea Tracker!</h2>
          
          <p class="lead mb-4">
            Would you like to import your tea collection from a CSV file?
          </p>

          <div class="alert alert-info mb-4">
            <h5 class="alert-heading">CSV File Requirements</h5>
            <p class="mb-2">Your CSV file must:</p>
            <ul class="mb-0">
              <li>Be less than 1MB in size</li>
              <li>Contain no more than 200 rows</li>
              <li>Have the following columns in this exact order:</li>
            </ul>
            <div class="mt-2">
              <code>name, category, price, vendor, known_for, year, region, ship_from, popularity, shopping_platform, product_url, weight, total_price</code>
            </div>
          </div>

          <div class="d-flex justify-content-center gap-3">
            <%= form_with url: upload_csv_teas_path, multipart: true, class: "d-inline" do |form| %>
              <div class="mb-3">
                <%= form.file_field :file, 
                    accept: '.csv',
                    class: "form-control",
                    required: true %>
              </div>
              <%= form.submit "Upload CSV", class: "btn btn-primary" %>
            <% end %>

            <%= link_to "Skip for Now", root_path, class: "btn btn-outline-secondary" %>
          </div>

          <div class="mt-3">
            <%= link_to sample_csv_teas_path, class: "btn btn-link" do %>
              <i class="bi bi-download me-2"></i>Download Sample Template
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div> 